# Domain docs

This repository uses a single context.

Before changing domain behavior, read `CONTEXT.md` if it exists and any relevant decisions under `docs/adr/`. Their absence is not an error; create them only when the domain language or an architectural decision needs to be recorded.

Use terminology defined in `CONTEXT.md` consistently. Surface any conflict with an existing ADR instead of silently overriding it.
